<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net9.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
    <NoWarn>$(NoWarn);NU1900</NoWarn>
    <AssemblyName>repoclean</AssemblyName>
    <AssemblyTitle>Repository Cleaner</AssemblyTitle>
    <AssemblyDescription>A tool to remove older, unused software items from a repository</AssemblyDescription>
    <AssemblyCompany>Emily Carr University</AssemblyCompany>
    <AssemblyProduct>Cimian</AssemblyProduct>
    <AssemblyVersion>2025.09.06.1400</AssemblyVersion>
    <FileVersion>2025.09.06.1400</FileVersion>
    <PublishSingleFile>true</PublishSingleFile>
    <SelfContained>true</SelfContained>
    <PublishTrimmed>false</PublishTrimmed>
    <IncludeNativeLibrariesForSelfExtract>true</IncludeNativeLibrariesForSelfExtract>
    <EnableWindowsTargeting>true</EnableWindowsTargeting>
  </PropertyGroup>

  <ItemGroup>
    <!-- Only include packages not already in Directory.Build.props -->
    <PackageReference Include="Newtonsoft.Json" Version="13.0.3" />
  </ItemGroup>

  <ItemGroup>
    <!-- Update YamlDotNet to newer version, overriding Directory.Build.props -->
    <PackageReference Update="YamlDotNet" Version="16.3.0" />
  </ItemGroup>

</Project>
