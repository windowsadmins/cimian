<Window x:Class="Cimian.Status.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        ui:WindowHelper.UseModernWindowStyle="False"
        Title="Cimian Status" 
        Height="550" 
        Width="600"
        MinHeight="550"
        MinWidth="500"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen"
        Icon="../cimian.ico"
        Background="{DynamicResource ApplicationPageBackgroundThemeBrush}"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        WindowStyle="None"
        AllowsTransparency="True">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        
        <!-- Close Button Style with Hover Effect -->
        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{DynamicResource SystemControlForegroundBaseHighBrush}"/>
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="HoverBorder" 
                                Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="HoverBorder" Property="Background" Value="#E81123"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="HoverBorder" Property="Background" Value="#C50E1F"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Window.RenderTransform>
        <TranslateTransform/>
    </Window.RenderTransform>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                   From="0" To="1" Duration="0:0:0.4">
                        <DoubleAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                    <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                                   From="20" To="0" Duration="0:0:0.5">
                        <DoubleAnimation.EasingFunction>
                            <CubicEase EasingMode="EaseOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>

    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="150" MinHeight="100"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Section with Modern Design -->
        <Grid Grid.Row="0" Margin="0,40,0,32">
            <!-- App Icon with 30px padding -->
            <Border Width="64" Height="64" 
                    CornerRadius="12"
                    Background="{DynamicResource ControlFillColorDefaultBrush}"
                    Margin="30,0,0,0"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center">
                <Border.Effect>
                    <DropShadowEffect Color="{StaticResource AccentColor}" 
                                    Direction="270" 
                                    ShadowDepth="0" 
                                    BlurRadius="12" 
                                    Opacity="0.3"/>
                </Border.Effect>
                <Image Source="../cimian.png" 
                       Width="64" Height="64"
                       RenderOptions.BitmapScalingMode="HighQuality"/>
            </Border>

            <!-- Centered Title Section (spans entire window width) -->
            <TextBlock Text="Managed Software Update" 
                      Style="{StaticResource TitleTextStyle}"
                      VerticalAlignment="Center" 
                      HorizontalAlignment="Center"/>

            <!-- Close Button -->
            <Button VerticalAlignment="Center"
                    HorizontalAlignment="Right"
                    Content="×"
                    Click="CloseWindow_Click"
                    Style="{StaticResource CloseButtonStyle}"
                    ToolTip="Close and stop updates"
                    Margin="0,0,40,0"/>
        </Grid>

        <!-- Main Status Card -->
        <Border Grid.Row="1" 
                Style="{StaticResource ModernCardStyle}"
                Background="{DynamicResource CardBackgroundFillColorDefaultBrush}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Progress Section (Always visible) -->
                <StackPanel Grid.Row="0" Margin="0,0,0,16">
                    <!-- Status Text -->
                    <TextBlock Text="{Binding StatusText}"
                              Style="{StaticResource SubtitleTextStyle}"
                              Margin="0,0,0,8"
                              HorizontalAlignment="Left"/>
                    
                    <!-- Progress Bar -->
                    <ProgressBar Value="{Binding ProgressValue}"
                                Style="{StaticResource ModernProgressStyle}"
                                Margin="0,0,0,8"
                                IsIndeterminate="{Binding IsIndeterminate}">
                        <ProgressBar.Foreground>
                            <SolidColorBrush Color="{Binding ProgressColor}"/>
                        </ProgressBar.Foreground>
                    </ProgressBar>

                    <!-- Progress Text -->
                    <Grid>
                        <TextBlock Text="{Binding ProgressText}"
                                  Style="{StaticResource CaptionTextStyle}"
                                  HorizontalAlignment="Left"/>
                        <TextBlock Style="{StaticResource CaptionTextStyle}"
                                  HorizontalAlignment="Right">
                            <TextBlock.Text>
                                <MultiBinding StringFormat="{}{0}%">
                                    <Binding Path="ProgressValue"/>
                                </MultiBinding>
                            </TextBlock.Text>
                        </TextBlock>
                    </Grid>
                </StackPanel>

                <!-- Spacer -->
                <Grid Grid.Row="1"/>
            </Grid>
        </Border>

        <!-- Live Log Viewer Section (Collapsible) -->
        <Border Grid.Row="2" 
                Style="{StaticResource ModernCardStyle}"
                Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}"
                Margin="0,16,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Log Viewer Header (Always Visible) -->
                <Button Grid.Row="0"
                        Background="Transparent"
                        BorderThickness="0"
                        Padding="16,12"
                        HorizontalContentAlignment="Center"
                        Click="ToggleLogViewer_Click">
                    <Grid HorizontalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0"
                                  Text="Log"
                                  Style="{StaticResource SubtitleTextStyle}"
                                  VerticalAlignment="Center"
                                  HorizontalAlignment="Center"
                                  Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"/>

                        <TextBlock Grid.Column="1"
                                  VerticalAlignment="Center"
                                  Margin="12,0,0,0"
                                  Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource BodyTextStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsLogViewerExpanded}" Value="True">
                                            <Setter Property="Text" Value="▲"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsLogViewerExpanded}" Value="False">
                                            <Setter Property="Text" Value="▼"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                </Button>

                <!-- Log Viewer Content (Collapsible) -->
                <Border Grid.Row="1"
                        Background="{DynamicResource ControlFillColorTertiaryBrush}"
                        CornerRadius="6"
                        Margin="12,0,12,12"
                        MinHeight="120"
                        MaxHeight="350">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsLogViewerExpanded}" Value="False">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsLogViewerExpanded}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>

                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" MinHeight="80"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Log Text Area -->
                        <ScrollViewer x:Name="LogScrollViewer"
                                      Grid.Row="0"
                                      VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Auto"
                                      Padding="12,12,12,8"
                                      CanContentScroll="False">
                            <TextBox x:Name="LogTextBox"
                                     Text="{Binding LogText, Mode=OneWay}"
                                     FontFamily="Consolas"
                                     FontSize="11"
                                     Foreground="{DynamicResource SystemControlForegroundBaseHighBrush}"
                                     Background="Transparent"
                                     BorderThickness="0"
                                     IsReadOnly="True"
                                     TextWrapping="NoWrap"
                                     AcceptsReturn="True"
                                     VerticalScrollBarVisibility="Disabled"
                                     HorizontalScrollBarVisibility="Disabled"
                                     SelectionBrush="{DynamicResource AccentColorBrush}"
                                     IsReadOnlyCaretVisible="True"/>
                        </ScrollViewer>

                        <!-- Copy Button -->
                        <Button Grid.Row="1"
                                Content="Copy Log"
                                HorizontalAlignment="Right"
                                Margin="12,4,12,8"
                                Padding="12,6"
                                Click="CopyLog_Click"
                                Style="{StaticResource SecondaryButtonStyle}"
                                FontSize="12"/>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>
